require.config({
    baseUrl: 'js',
    paths : {
        Student : 'student',
        BaseController : 'BaseController',
        
    }
});

require(['BaseController'], function (BaseController) {
    
});
